#### 关键字集合

#### mutating：变异方法。

1、主要用于值类型（struct、enum）中的方法修改该类型中的属性。

#### self

1、类型中每一个实例的一个隐藏属性，它等同于实例本身，用于区别方法中的形参名称和类型中的属性名称。

2、还可以在可变方法中给self重复赋值一个实例。

#### static

1、用于修饰值类型的类型方法。
2、在类型方法中，self指向这个类型本身。

